<Window x:Class="ClinicStatisticsApp.UI.SummaryProDoctorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Отзывы ПроДокторов"
        Height="900"
        Width="1500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Год:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="YearComboBox" Width="100" Margin="0,0,20,0"/>

            <Button Content="Загрузить" Width="140" Height="34" Margin="0,0,10,0" Click="LoadButton_Click"/>
            <Button Content="Сохранить QR" Width="140" Height="34" Click="SaveButton_Click"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="BlocksPanel"/>
        </ScrollViewer>
    </Grid>
</Window>